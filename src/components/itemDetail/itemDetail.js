export const ItemDetail = (props) => {
  console.log(props.title);
  return (
    <div className="items">
      {/* <img src={props.picture} />
      <h4>{props.title}</h4>
      <ul>
        <li>
          <strong>Precio $ {props.price}</strong>
        </li>
        <li>
          <strong>Estado: {props.condition}</strong>
        </li>
      </ul> */}
      {/* <ItemCount /> */}
    </div>
  );
};
